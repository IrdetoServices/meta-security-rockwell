diff --git a/source/firewall_log/GenFWLog.c b/source/firewall_log/GenFWLog.c
index 4d6d774..ef28a30 100644
--- a/source/firewall_log/GenFWLog.c
+++ b/source/firewall_log/GenFWLog.c
@@ -113,7 +113,7 @@ int fLock(){
     fl.l_len = 0;  
     fl.l_pid = -1;  
 
-    if((fd=open(LOCK_FILE_NAME,O_CREAT| O_RDWR))>0){
+    if((fd=open(LOCK_FILE_NAME,O_CREAT| O_RDWR, 0644))>0){
         do{
             if( -1  != fcntl(fd,F_SETLK,&fl)){
                 printf("GET W-LOCK SUCCESS\n");
diff --git a/source/syscfg/lib/syscfg_lib.c b/source/syscfg/lib/syscfg_lib.c
index a16cb19..abd7aa3 100644
--- a/source/syscfg/lib/syscfg_lib.c
+++ b/source/syscfg/lib/syscfg_lib.c
@@ -1540,7 +1540,7 @@ int commit_to_file (const char *fname)
     char buf[2*MAX_ITEM_SZ];
     syscfg_shm_ctx *ctx = syscfg_ctx;
 
-    fd = open(fname, O_CREAT | O_RDWR | O_TRUNC);
+    fd = open(fname, O_CREAT | O_RDWR | O_TRUNC, S_IRWXU);
     if (-1 == fd) {
         return ERR_IO_FILE_OPEN;
     }
diff --git a/source/syscfg/lib/syscfg_mtd.c b/source/syscfg/lib/syscfg_mtd.c
index 0b20bcb..4e48ced 100644
--- a/source/syscfg/lib/syscfg_mtd.c
+++ b/source/syscfg/lib/syscfg_mtd.c
@@ -218,7 +218,7 @@ int mtd_read_to_file (const char *mtd_device, const char *file)
         // fprintf(stderr, "%s: Error invalid mtd syscfg size %d\n", __FUNCTION__, mtd_hdr.size);
         goto err_mtd_read;
     }
-    if (-1 == (out_fd = open(file, O_CREAT | O_WRONLY | O_TRUNC))) {
+    if (-1 == (out_fd = open(file, O_CREAT | O_WRONLY | O_TRUNC, S_IRWXU))) {
         // perror("mtd_read: out file open()");
         rc = ERR_IO_FILE_OPEN;
         goto err_mtd_read;
diff --git a/source/utapi/lib/utapi.c b/source/utapi/lib/utapi.c
index a911b6f..1ad342d 100644
--- a/source/utapi/lib/utapi.c
+++ b/source/utapi/lib/utapi.c
@@ -6118,7 +6118,7 @@ static pid_t s_spawn_async_process(const char *lock_filename,
         struct flock fl; // Lock settings
         
         // Open the lock file
-        lock_fd = open(lock_filename, O_WRONLY|O_CREAT);
+        lock_fd = open(lock_filename, O_WRONLY|O_CREAT, 0644);
         
         if (lock_fd < 0) {
             exit(1);
@@ -6345,7 +6345,7 @@ int Utopia_DiagTracerouteTestStart (char *dest)
         0 == strcmp(dest, "255.255.255.255"))
     {
         // Open the log file
-        fd = open(TRACEROUTE_LOG_TMP_FILE, O_WRONLY|O_CREAT);
+        fd = open(TRACEROUTE_LOG_TMP_FILE, O_WRONLY|O_CREAT, 0644);
         
         if (fd < 0) {
             return ERR_FILE_NOT_FOUND;
